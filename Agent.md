# Agent.md

## 0. このリポジトリでのCodexの役割
Codex は、ユーザーの指示をもとに、リポジトリを調査し、実装計画を立て、承認済み範囲のコード変更・確認・ローカルコミットを進めてよい。
ただし、push、PR作成、破壊的操作、仕様未確定領域の確定実装は、ユーザーの明示承認なしに行ってはいけない。

---

## 1. Codex app / Windows 前提

Codex app は Windows 環境でも使用される前提で作業する。

Windows や PowerShell では、日本語が文字化けして見える場合がある。  
文字化けに見えても、ファイル本体が壊れていると決めつけない。

守ること。

- UTF-8 BOM を付けない。
- 文字化け疑い箇所を勝手に修正しない。
- 日本語ファイル名・日本語ディレクトリ名を勝手に変更しない。
- 改行コードや文字コードの一括変換をしない。
- PowerShell表示由来の問題を、コード破損として扱わない。
- Agent.mdを改変しない。

---

## 2. 作業前に読む正本

 - "Production-logic\combination\00_PROJECT_DOCS_INDEX.md"

---

## 3. 明示承認なしで可能な操作

以下は、ユーザーの追加承認なしで実行してよい。

- プロジェクト内のファイルを読む。
- `AGENTS.md` と設計資料を確認する。
- `rg` などで関連箇所を調査する。
- 現状監査を行う。
- 影響範囲を整理する。
- 実装計画を作る。
- 非破壊の確認コマンドを実行する。
- サブエージェントGPT-5.3-Codex-Sparkの活用。ただし、成果物の確認はすること。

例。

- `git status`
- `git diff`
- `git log`
- `rg`
- `Get-ChildItem`
- `Get-Content`
- ファイル内容確認
- 構文確認
- 参照パス確認

---

## 4. 実装承認の扱い

Codex は承認済み範囲内で、必要な一部置換・局所修正・関連ドキュメント更新・確認・小さなローカルコミットまで進めてよい。

---

## 5. 明示承認が必要な操作

以下は、ユーザーの明示承認なしに行ってはいけない。

- 1ファイル全体の置換。
- 複数ファイルの一括置換。
- 文字化け疑い箇所の修正。
- Git履歴の書き換え。
- rebase。
- reset。
- force push。
- push。
- PR作成。

---

## 6. 現在のリポジトリ状態

 - まだ作業手順マークダウンのみ。

---

## 7. プロジェクトの芯

このプロジェクトは、ダビスタ風競馬シミュレーションゲームである。

- 配合機能
- 既存の種牡馬・繁殖牝馬を使って生産
- 生産馬を引退後に繁殖入りさせ、次世代配合ができる
- 調教機能
- 生産馬を育成する
- 能力を伸ばす
- レース機能
- 生産馬をレースで走らせる
- コード出力・友人対戦
- 育てた馬をコード化
- 友人の馬コードを読み込んで対戦できる

---

## 8. 最優先項目

現在の優先項目は配合機能及び種牡馬/繁殖牝馬を用いた生産による馬のパラメータ決定ロジックです。
「配合 → 生産 → 能力表示」を確定し、馬を生産して能力を見れるところまで進めてください。

---

## 9. 絶対に守るルール

1.Agent.mdを変えない。
2.既存のマークダウンファイルの勝手な更新。
3.どこまで実装できていてどこからがまだできていないのかを誰が見てもわかる形に残す。
4.張りぼて状態にせずに見た目からわかりやすいゲームデザイン。
5.レイアウトの崩れの確認。
6.小さくローカルコミットをする。

---

## 10. Git / commit ルール

各作業ごとに小さくローカルコミットをする。
ただし、push と PR作成はユーザーの明示指示があるまで行わない。

コミット前に確認する。

- `git diff`
- `git status`
- 意図しないファイル変更がないこと
- 文字コード変更が混ざっていないこと
- 改行コードの一括変更が混ざっていないこと
- 関連ドキュメント更新漏れがないこと

PowerShell の `git add` が `.git/index.lock` 周りで失敗する場合は、`.git/index.lock` を削除せず、Codex の Node REPL から `child_process.execFile("git", ["add", "--", ...対象ファイル])` で対象ファイルだけを stage する。

---

## 11. 報告形式

作業完了時は、以下を簡潔に報告する。

- 何を確認したか
- 何を変更したか
- どのファイルを変更したか
- 実行したテスト・確認
- 実行できなかった確認と理由
- ローカルコミットを作成した場合は、その概要
- 残課題
- 次に行うべき作業

---

## 12. 環境の構築

実装に際して必要な環境がある場合は適宜ユーザーに提案する。
